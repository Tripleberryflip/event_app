<div class="container">
  <%= form_for(@event_coordinator) do |f| %>
    <% if @event_coordinator.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@event_coordinator.errors.count, "error") %> prohibited this event_coordinator from being saved:</h2>

        <ul>
        <% @event_coordinator.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= f.text_field :name, :placeholder => "Name" %>

      <%= f.text_field :email, :placeholder => "Email" %>

      <%= f.password_field :password, :placeholder => "Password" %>

      <%= f.password_field :password_confirmation, :placeholder => "Password Confirmation" %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>
</div>
