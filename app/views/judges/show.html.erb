<% provide(:title, 'Judge Profile') %>
<div class="container">
	<center><p id="notice"><%= notice %></p>

	<p>
	  <b>Name:</b>
	  <%= @judge.name %>
	  <br />
		<b>Email:</b>
	  <%= @judge.email %>
	  <br />
		<b>Event:</b>
	<%= @judge.event_id %>
		
	</p>

		<div class="container">
			<%= render :partial => 'events/list', :locals => {:judge => @judge } %>
		</div>

		<!-- <div class="container">
			<= render :partial => 'competitors/index', :locals => {:judge => @judge } %>
		</div>-->


	<%= link_to 'Edit', edit_judge_path(@judge) %> |
	<%= link_to 'Back', judges_path %><center>
</div>
