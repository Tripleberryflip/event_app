<div id="new_competitor">
  <%= form_for(@competitor, remote: true) do |f| %>
    <% if @competitor.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@competitor.errors.count, "error") %> prohibited this competitor from being saved:</h2>

        <ul>
        <% @competitor.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <div class="ui-input-text ui-shadow-inset ui-corner-all ui-btn-shadow ui-body-c">
        <%= f.text_field :name, :placeholder => 'Name', :class => "ui-input-text ui-body-c" %>
      </div>
      
      <%= f.hidden_field :event_id %>
    </div>
    <div class="actions">
      <div data-corners="true" data-shadow="true" data-iconshadow="true" data-wrapperels="span" data-theme="c" data-disabled="false" class="ui-submit ui-btn ui-shadow ui-btn-corner-all ui-btn-up-c" aria-disabled="false">
        <span class="ui-btn-inner">
          <span class="ui-btn-text">Create Competitor</span>
          <%= f.submit :class=>"ui-btn-hidden"%>
        </span>
      </div>
    </div>
  <% end %>
</div>
